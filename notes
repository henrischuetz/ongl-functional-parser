use bottom up parsing
context free grammar

lexer
breaks up the string into chuncks "lexemes"
build small blocks like #/\d|\w/ ... some thing like that and give it a tag => VARIABLE
= => ASSIGNMENT, /"\w|\d"/ => VALUE 
which then means VARIABLE ASSIGNMENT VALUE ==> assigning a value to a variable


implement operators + - * / in the context of expressions...

block try custom function that detects if theres another block